## Datum Connect local environment
##
## Copy this to `.env` (repo root) or export these variables in your shell.

# Required for publishing/fetching tickets via n0des (codename-based join).
# If unset, the app/CLI will run in "local-only" mode and skip n0des integration.
N0DES_API_SECRET=

# Optional: override where Datum Connect stores its repo/state on disk (CLI).
DATUM_CONNECT_PATH=

